import { AdjustAssetPathPipe } from './adjust-asset-path.pipe';

describe('AdjustAssetPathPipe', () => {
  it('create an instance', () => {
    const pipe = new AdjustAssetPathPipe();
    expect(pipe).toBeTruthy();
  });
});
