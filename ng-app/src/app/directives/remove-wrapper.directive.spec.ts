import { RemoveWrapperDirective } from './remove-wrapper.directive';

describe('RemoveWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
